spring.application.name=gerenciadorTarefas

# Configura��o do H2 para uso em mem�ria
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=1234
spring.sql.init.platform=h2

# Configura��o do Hibernate para o banco H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# Ativar o console do H2 (opcional, �til para depura��o)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Configura��es extras para melhorar o desempenho (opcional)
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true